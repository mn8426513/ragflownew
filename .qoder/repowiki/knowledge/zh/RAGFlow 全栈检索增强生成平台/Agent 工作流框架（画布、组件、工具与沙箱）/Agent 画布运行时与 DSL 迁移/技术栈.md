Python asyncio 异步生成器作为事件流驱动；使用 `contextvars` 在协程/线程间传播 LLM 请求上下文与 token 用量；通过 `concurrent.futures.ThreadPoolExecutor` 与 `asyncio.Semaphore` 混合执行同步组件；TTS 基于 `rag.utils.tts_cache.synthesize_with_cache` 缓存合成。