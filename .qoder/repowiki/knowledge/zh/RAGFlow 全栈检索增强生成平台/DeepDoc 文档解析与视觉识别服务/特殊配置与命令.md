启动统一模型服务：`python deepdoc/server/deepdoc_server.py --port 9390 --model-dir <路径>`，可用 `--disable-dla/--disable-ocr/--disable-tsr` 按需关闭端点；OCR 推理线程数由 `OCR_INTRA_OP_NUM_THREADS`、`OCR_INTER_OP_NUM_THREADS` 控制；布局识别后端通过 `LAYOUT_RECOGNIZER_TYPE=ascend|onnx` 切换；测试脚本 `python deepdoc/vision/t_ocr.py` 与 `python deepdoc/vision/t_recognizer.py --mode={layout,tsr}` 可验证 OCR 与版面/表格识别效果。