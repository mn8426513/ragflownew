进程入口支持 `--version`、`--debug`、`--init-superuser` 三个 CLI 参数；可通过环境变量 `RAGFLOW_DEBUGPY_LISTEN` 启用 debugpy 远程调试，`QUART_RESPONSE_TIMEOUT`/`QUART_BODY_TIMEOUT`/`MAX_CONTENT_LENGTH` 调整超时与上传大小；启动时自动执行 `init_web_db()` 建表与 `init_web_data()` 填充初始数据。