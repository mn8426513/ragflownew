Python ≥3.13（`requires-python = ">=3.13,<3.14"`），HTTP 客户端基于 `requests`，运行时类型校验通过 `beartype` 的 `beartype_this_package()` 全局注入；构建与依赖管理使用 `uv`（见 `uv.lock`），测试依赖 pytest、hypothesis、openpyxl、python-docx、python-pptx、reportlab、requests-toolbelt。