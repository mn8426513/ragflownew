- 通过 pytest 的 `--level` 选项将 p1/p2/p3 映射为 marker 表达式，在 `pytest_configure` 中统一注入 `markexpr`，实现测试分级执行。
- 所有外部依赖（如 rag.llm、scholarly）在 `conftest` 中以动态 stub 模块形式注入，避免导入时加载真实 LLM/网络依赖。
- 测试配置集中放在 `configs.py`，优先从环境变量读取，其次回退到 `docker/.env` 文件解析，再 fallback 到默认值。
- 会话级 fixture（auth/token/set_tenant_info）在首次使用时自动完成用户注册、登录、模型工厂与实例绑定，并以幂等方式处理“已存在”错误。
- 对 Go 代理与 Python 代理返回差异（JSON vs text、provider_name vs model_provider）通过 `API_PROXY_SCHEME`/`IS_GO_PROXY` 分支兼容处理。
- Playwright 测试将选择器、等待条件、认证步骤拆分为 `helpers/auth_selectors`、`auth_waits`、`flow_steps` 等模块，测试文件仅编排高层 flow。