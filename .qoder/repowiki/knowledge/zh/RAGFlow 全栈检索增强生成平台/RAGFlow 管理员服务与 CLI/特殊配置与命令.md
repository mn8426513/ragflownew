服务端通过 `python admin/server/admin_server.py` 直接运行，监听 0.0.0.0:9381；默认管理员账号由 `DEFAULT_SUPERUSER_EMAIL`、`DEFAULT_SUPERUSER_NICKNAME`、`ADMIN_DEFAULT_PASSWORD`/`DEFAULT_SUPERUSER_PASSWORD` 环境变量引导生成，若未设置则随机生成密码写入 `logs/admin_bootstrap_password.txt`（权限 0600）。CLI 通过 `python -m admin.client.ragflow_cli` 或打包后的可执行文件进入交互模式，支持 `login`、`list users`、`create role` 等自定义命令。