Python: click + rich + tqdm (ES→OB 迁移)、aiohttp + dataclasses (Firecrawl)、peewee + peewee-migrate (db_schema_sync)；Go: 标准库 flag + encoding/json 反射生产组件清单；Shell: git sparse-checkout/sparse-clone 拉取外部测试数据；构建系统: hatchling (`pyproject.toml`) 与 uv.lock。