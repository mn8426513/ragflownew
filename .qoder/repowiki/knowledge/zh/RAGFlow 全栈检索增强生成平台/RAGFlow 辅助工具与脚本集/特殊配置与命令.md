`es-ob-migrate` 通过 `uv run es-ob-migrate` 或安装后直接调用，子命令包括 `migrate`/`schema`/`verify`/`list-indices`/`list-kb`/`status`/`sample`；`db_schema_sync.py` 需指定 `--host/--port/--user/--password/--database/--version` 并选择 `--list/--create/--migrate/--diff`（可选 `--drop` 含删除列）；`fetch_deepdoc_testdata.sh <pkg>` 依赖 `RAGFLOW_TESTDATA_REPO`/`RAGFLOW_TESTDATA_REF`/`XDG_CACHE_HOME` 环境变量；`gen-component-parity` 与 `migrate-canvas` 通过 `go run ./tools/<tool>` 执行，后者支持 `-golden` 与 `-walk` 模式。