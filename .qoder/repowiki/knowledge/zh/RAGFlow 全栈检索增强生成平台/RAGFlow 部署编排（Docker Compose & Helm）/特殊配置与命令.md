单机部署使用 `docker compose --profile cpu|gpu up -d` 启动，默认加载 `.env`；启用 OceanBase 需先在宿主机设置 PAM limits（nofile 655350、core unlimited）并确认 `ulimit -n` 生效。Kubernetes 部署通过 `helm install <release> ./helm -f values.yaml` 完成，可通过 `values.yaml` 中的 `global.repo` 统一替换镜像仓库前缀，`ingress.enabled` 控制外部访问入口。