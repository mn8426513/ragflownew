根目录通过 `uv sync --python 3.13 --all-extras` 安装 Python 依赖、`cd docker && docker compose -f docker-compose.yml up -d` 拉起完整栈、`bash docker/launch_backend_service.sh` 启动 Python 后端双进程、`./run_go_tests.sh` 运行 Go 测试、`uv run pytest` 运行 Python 测试套件。